// Package validator provides a high-level, user-friendly validator API.
//
// It exposes simple helpers (Validate, ValidateMap, ValidateWithResult) and
// types to validate arbitrary map/struct data against string-based rules
// similar to popular validation frameworks. See the README for examples.
package validator
